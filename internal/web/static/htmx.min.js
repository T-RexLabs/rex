/*!
 * htmx vendored placeholder.
 *
 * V1 ships htmx 2.0.x — drop the real minified bytes in place of this
 * file. The skeleton renders fully without htmx (web-ui.ACCESS.3:
 * "the UI degrades gracefully on JS-disabled browsers — read paths
 * fully work").
 *
 * Replace with the contents of:
 *   https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js
 *
 * The htmx-ext-sse extension lives at static/htmx-ext-sse.js and
 * follows the same model.
 *
 * Until then this file is a no-op so the <script src> tag does not
 * 404.
 */
(function () {
  if (typeof window === 'undefined') return;
  if (window.htmx) return;
  window.htmx = {
    version: 'placeholder',
    config: {},
    on: function () {},
    off: function () {},
    process: function () {},
  };
})();
