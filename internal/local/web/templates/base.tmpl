{{define "base"}}<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="color-scheme" content="light dark">
  <title>{{block "title" .}}rex{{end}}</title>
  <link rel="stylesheet" href="/static/app.css">
  <link rel="stylesheet" href="/static/chroma.css">
</head>
<body>
  <header class="topbar">
    <div class="topbar-inner">
      <a class="brand" href="/">rex</a>
      <nav class="nav" aria-label="primary">
        <a href="/"         {{if eq .NavSection "home"}}aria-current="page"{{end}}>overview</a>
        <a href="/specs"    {{if eq .NavSection "specs"}}aria-current="page"{{end}}>specs</a>
        <a href="/runs"     {{if eq .NavSection "runs"}}aria-current="page"{{end}}>runs</a>
        <a href="/audit"    {{if eq .NavSection "audit"}}aria-current="page"{{end}}>audit</a>
        <a href="/remotes"  {{if eq .NavSection "remotes"}}aria-current="page"{{end}}>remotes</a>
        <a href="/settings" {{if eq .NavSection "settings"}}aria-current="page"{{end}}>settings</a>
      </nav>
      <form class="topbar-search" method="get" action="/search" role="search">
        <input class="topbar-search-input"
               type="search"
               name="q"
               placeholder="search…"
               aria-label="search workspace"
               autocomplete="off">
      </form>
      {{if .Workspace}}
        <span class="ws-badge" title="active workspace">{{.Workspace.ID}}</span>
      {{else}}
        <span class="ws-badge muted" title="no workspace at this path">no workspace</span>
      {{end}}
    </div>
  </header>
  <main class="main">
    {{block "content" .}}{{end}}
  </main>
  <footer class="footer">
    <span>rex {{.Version}}</span>
    <span>local · {{.BindAddr}}</span>
  </footer>
  <script src="/static/sort.js" defer></script>
</body>
</html>{{end}}
