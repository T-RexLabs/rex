# Build artifacts
/bin/
/dist/
*.test
*.out
coverage.txt
coverage.html

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local tooling state (per-machine, ephemeral)
# - .claude: Claude Code plugin state (e.g. installed skills like
#   the playwright-cli skill bundle).
# - .playwright, .playwright-cli: playwright-cli session data,
#   per-machine config, and the screenshot/console log artifacts
#   it leaves behind while driving a browser.
.claude/
.playwright/
.playwright-cli/

# Local config not checked in
/.rex/
*.local.yaml

# Local env files
.env
.env.local
