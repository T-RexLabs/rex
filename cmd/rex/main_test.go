package main

// Smoke tests for the rex binary live in internal/local/cli where the
// command tree is constructed; this file is intentionally empty so
// `go test ./...` still walks cmd/rex without surfacing a "no tests"
// note for what is a pure binary entry point.
