# Spec-bookkeeping reconciliation pass — cli.spec-commands done.
#
# Status: accepted.
# Date:   2026-05-08.
# Folded: 2026-05-08 — cli.spec-commands flipped to done after
# `rex spec edit` shipped. All six cli.SPEC.* requirements are now
# implemented:
#
#   SPEC.1  rex spec create            (existing)
#   SPEC.2  rex spec list              (existing)
#   SPEC.3  rex spec show              (existing)
#   SPEC.4  rex spec validate          (existing)
#   SPEC.5  rex spec edit              (this commit)
#   SPEC.6  rex spec acid              (existing)
#
# `rex spec edit` resolves the spec, opens it in $EDITOR (default
# vi), and re-validates strictly on save — non-zero exit when
# validation surfaces errors. Mirrors the workspace.yaml round-trip
# pattern but doesn't need yaml.Node here since it's the user's
# editor making changes, not us.

target: cli
amendment_kind: additive

changes:
  - kind: update-task-state
    task_id: spec-commands
    from: todo
    to: done
    proof: "internal/local/cli/spec.go now wires `rex spec edit <id>` (newSpecEditCmd) — all six cli.SPEC.* requirements are implemented; covered by 3 tests in spec_edit_test.go using a /bin/sh fake-editor fixture."
